// selectors.js
// export const selectTotalCost = () => {
//   const { products } = state.basketItems; // Замените на фактическую структуру вашего состояния
  // const basketItems = products.slice(0, 4); // Измените в соответствии с вашей логикой получения товаров в корзине
//   return products.reduce((total, item) => {
//     return total + (item.discont_price || item.price);
//   }, 0);
// };
